apiVersion: v1
kind: Service
metadata:
  name: jenkins-loadbalancer
  labels:
    app: jenkins-server
  namespace: tooling
spec:
  type: LoadBalancer
  selector:
    app: jenkins-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080